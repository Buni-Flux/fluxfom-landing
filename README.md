# FluxFom - Brand Positioning & Growth Studio | Nairobi

Standalone FluxFom app extracted from the original monorepo.

## Local setup

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` and fill in the Supabase values before using authenticated features.
